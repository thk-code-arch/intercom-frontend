export default class User {
  constructor(username, email, password, invitecode) {
    this.username = username;
    this.email = email;
    this.password = password;
    this.invitecode = invitecode;
  }
  }
